import styled from 'styled-components';

const Image = styled.img`
  box-shadow: 1px 1px 3px 0px rgba(0, 0, 0, 0.5);
  border: 2px solid rgb(28, 181, 224);
  border-radius: 2px;
  max-width: 300px;

  margin-bottom: 5px;
`;

export default Image;
